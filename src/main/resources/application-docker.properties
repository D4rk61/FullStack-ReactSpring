spring.application.name=login
# Ejecutar en local
#spring.datasource.url=jdbc:postgresql://localhost:5432/recipes
#spring.datasource.username=postgres
#spring.datasource.password=1234

# Docker
spring.datasource.url=${DATABASE_URL}
spring.datasource.username=${DATABASE_USERNAME}
spring.datasource.password=${DATABASE_PASSWORD}
spring.datasource.hikari.driver-class-name=org.postgresql.Driver
# Hibernate & SQL Settings
spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Server settings
storage.location=assets
server.error.include-stacktrace=never
server.port=8080

# Thymeleaf settings
#spring.thymeleaf.cache=false
#spring.servlet.multipart.max-file-size=99MB
# Loggin settings

# JWT
jwt.secret.key=${JWT_SECRET_KEY}
jwt.secret.time=${JWT_EXPIRATION}

# email config
email.email=reynosojose2005@gmail.com
email.passw=btdcnczvzkbgpgcv

# jackson
spring.jackson.serialization.write-dates-as-timestamps=false

logging.level.root=error
logging.level.org.hibernate=info

